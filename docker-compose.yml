version: '3.8'

volumes:
  pg_app:

services:
  db:
    image: mysql:8.0.33
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: security_aws_db_docker
      MYSQL_USER: admin
      MYSQL_PASSWORD: root
    volumes:
      - pg_app:/var/lib/mysql
    ports:
      - "3307:3306"